<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>タスク一覧</title>
  <%= stylesheet_link_tag "application", media: "all", "data-turbolinks-track": "reload" %>
  <%= javascript_include_tag "application", "data-turbolinks-track": "reload" %>
</head>
<body>
  <div class="container mt-5">
    <h1 class="mb-4">タスク一覧</h1>

    <table class="table">
      <thead>
        <tr>
          <th>タイトル</th>
          <th>完了状況</th>
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
        <% @todos.each do |todo| %>
          <tr>
            <td><%= todo.title %></td>
            <td><%= todo.is_done ? '完了' : '未完了' %></td>
            <td><%= link_to '詳細', todo, class: 'btn btn-primary' %></td>
            <td><%= link_to '編集', edit_todo_path(todo), class: 'btn btn-warning' %></td>
            <td><%= button_to '削除', todo, data: { confirm: '本当に削除しますか？' }, method: :delete, class: 'btn btn-danger' %></td>
          </tr>
        <% end %>
      </tbody>
    </table>

    <br>

    <%= link_to '新規タスク作成', new_todo_path, class: 'btn btn-success' %>
  </div>
</body>
</html>